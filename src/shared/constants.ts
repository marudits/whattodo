export const CONSTANTS = {
    AUTH: {
        MODE: {
            SIGN_IN: 'SIGN_IN',
            SIGN_UP: 'SIGN_UP'
        }
    },
    FORM: {
        EMAIL: {
            FORMAT: /^[A-Za-z0-9\-\+\_]+@[a-zA-Z0-9_-]+(\.[a-zA-Z0-9_-]+)+$/
        },
        PASSWORD: {
            MIN_LENGTH: 8
        }
    }
}